{
  "agent": "B",
  "state": "done",
  "outputs": [
    "brain/decide.py",
    "brain/coverage.py",
    "brain/metadata.py",
    "brain/variance_io.py",
    "brain/test_decide.py",
    "brain/fixtures/variance_fixture.json",
    "brain/fixtures/live_index_fixture.json"
  ],
  "smoke_test": {
    "cmd": ".venv/bin/python brain/test_decide.py",
    "exit_code": 0,
    "output_tail": "ALL PASS"
  },
  "notes": "AMENDMENT (orchestrator-approved, in-session): Agent A's real variance/results/variance.json (now present, 50/50 tasks) showed zero deconfounded tasks with perm_p<=0.05 -- best real case is p~=0.11 (garment-inside-out, fabric-layering, garment-carton-packing, all 8-1/7-1 majority/outlier splits). The original perm_p<=0.05 ask-gate therefore never fires on this corpus. Changed tier=ask rule in brain/decide.py's _matched_result(): ask iff task_variance present AND not excluded AND family_confounded==False AND camera_confounded==False AND both k=2 clusters non-empty (min cluster size > 0) AND silhouette_k2>=0.1; else tier=act (missing/excluded-variance fallback unchanged from before). Reframed the ask as majority-vs-outlier: evidence and utterance_slots now use cluster_maj_n/cluster_min_n (replacing the old cluster_a_n/cluster_b_n and the removed 'k' slot); utterance_slots adds rounded 2dp 'silhouette' and 'perm_p'. Everything still computed from variance.json/metadata, no hardcoded task names (unchanged no-hardcoding source-scan test still passes). Verified against the REAL variance.json (default path, no override) for all 5 orchestrator-specified cases: tier=ask for garment-inside-out (sil 0.1524), fabric-layering (sil 0.1244), garment-carton-packing (sil 0.1221); tier=act for garment-iron-press (sil 0.0625) and drilling (sil 0.026). All 5 match exactly. Rewrote brain/fixtures/variance_fixture.json (real task_ids not among the 5 verified above, to avoid confusion with real numbers): belly-band-assembly = ask-eligible synthetic case; axle-shaft-cutting = confounded -> act despite silhouette>=0.1; garment-quality-checking = new edge case, all-one-label degenerate clustering (silhouette>=0.1 but minority cluster empty) -> act, added test_degenerate_single_cluster_falls_back_to_act to cover the 'both clusters non-empty' guard explicitly; processing-fabric-cut = excluded -> act. Added test_real_variance_ask_cases and test_real_variance_act_cases exercising decide() against the live variance/results/variance.json for the 5 named tasks. Bottle regression guard re-verified unaffected: bottle-cleaning is excluded in the real file (detection_rate 0.036) -> act fallback; bottle-surface-buffing and water-filtration-bottle-filling are both family+camera confounded in the real file -> act; buttonhole-stitching is camera_confounded -> act; none abstain, none incorrectly ask. Full smoke suite (63 checks) re-run after the amendment: exit 0, ALL PASS.",
  "ts": "2026-08-30T13:03:13-07:00"
}
